# Canonical URL for Open Graph / link previews (no trailing slash)
NEXT_PUBLIC_SITE_URL=https://mraduljain.vercel.app

# WhatsApp link for footer (country code + number, no + or spaces)
NEXT_PUBLIC_WHATSAPP_PHONE=919876543210

# Optional: Google Calendar appointment schedule booking page (calendar.app.google/...)
# When set, footer "Book a call" uses this instead of the calendar invite template.
NEXT_PUBLIC_GOOGLE_BOOKING_URL=
